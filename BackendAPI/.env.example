# BackendAPI environment configuration example

# Server
API_HOST=0.0.0.0
API_PORT=8000

# Database
DATABASE_URL=postgresql+psycopg://postgres:postgres@database:5432/music

# Auth
JWT_SECRET=change-me
JWT_ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=60

# CORS
CORS_ORIGINS=["http://localhost:3000"]

# Observability
# Toggle to enable log/metric forwarding to Monitoring&Logging
OBS_ENABLED=true
# Preferred variable for Observability base URL (protocol+host:port)
OBS_BASE_URL=http://localhost:8083
# API key for Monitoring&Logging service (set by operator)
OBS_API_KEY=replace-with-token
# Optional: legacy alias - if set, backend normalizes to use OBS_BASE_URL/OBS_ENDPOINT appropriately
# OBS_ENDPOINT=http://localhost:8083
OBS_SERVICE_NAME=backend-api
OBS_ENVIRONMENT=development
